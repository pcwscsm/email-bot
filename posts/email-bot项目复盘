# email-bot 项目复盘：从脚本到工程化系统的成长之路

本项目（pcwscsm/email-bot）是一个多源信息聚合机器人，旨在通过 LLM 摘要技术打破校园信息差。以下是开发过程中的核心思考与经验总结。

## 1. 从一次凭证泄露看个人项目的安全管理

在项目早期（5月2日），曾不慎将包含真实凭证的 `config.json` 推送到公开仓库。这次事故促使我重新思考个人项目的凭证管理逻辑。

* **对策：物理隔离与 Git 保护**
    * **配置文件外移**：将 `config.json` 移出项目代码目录，强制存储在操作系统的用户主目录（`USERPROFILE/EmailBot`）下 [cite: 4]。
    * **严苛的忽略规则**：在 `.gitignore` 中明确禁止任何配置文件上传。
    * **预检查机制**：在推送代码前，使用脚本检查敏感关键词（如 `password`, `api_key`）。
* **经验沉淀**：即便是个人的实验性项目，也必须在第一行代码写下之前就确定凭证隔离策略。

## 2. 为什么选择本地部署而非云端

虽然 GitHub Actions 或云服务器能提供 7×24 小时在线，但我最终选择了基于 Windows 任务计划程序的本地部署方案 [cite: 4]。

* **决策逻辑**：
    * **凭证安全 > 在线时长**：本地部署意味着敏感的邮箱授权码和学校 MIS 凭证永远不出个人电脑。
    * **维护成本**：云端环境需要额外处理网络代理和防火墙问题，而本地运行环境与开发环境高度一致。
    * **成本考量**：充分利用闲置的个人电脑算力，无需支付额外的云端资源费用。
* **代价与平衡**：虽然需要电脑常开，但对于个人信息中枢而言，安全性和易维护性是更核心的指标。

## 3. 工程师的“该弃就弃”：沉没成本的断舍离

在开发学院通知爬虫模块时，我投入了大量精力编写 CSS 选择器，但在集成阶段决定将其弃用。

* **反思过程**：
    * **低信噪比**：学院通知大多为仪式性内容，真正高价值的信息极少。
    * **资源浪费**：解析复杂 HTML 产生的 Token 消耗与信息价值不成正比。
* **经验沉淀**：识别“沉没成本”是开发者成熟的标志。事先设定放弃标准，把“验证此路不通”也视为一种项目产出，能有效避免项目走向过度复杂化。

## 4. 从单文件脚本到可扩展系统：Source 抽象的演化

随着数据源的增加，代码从 v1.0 的面向过程重构成为了 v1.4 的面向对象架构 [cite: 4]。

* **抽象契约设计**：
    * 定义了 `Source` 基类，规定所有数据源必须实现 `fetch()` 接口，且必须是无副作用的纯函数 [cite: 4]。
    * 引入了 `IMAPSource`（邮件）、`WebSource`（网页爬虫）和 `JSONAPISource`（结构化接口）三种子类实现 [cite: 4]。
* **稳定性保障**：
    * 通过 `golden_test.py` 确保重构不改动输出形状 [cite: 1]。
    * 针对网页和接口分别建立 `test_websource.py` [cite: 3] 和 `test_jsonapi.py` [cite: 2] 进行单元测试，模拟网络失败与数据异常。
* **收益**：这种架构让后续接入“北交大宣讲会”等新功能变得极其丝滑。

## 5. 侦察胜于蛮力： AJAX 接口的合法侦察

在处理北交大就业网这种使用 JS 动态渲染的页面时，我没有选择沉重的自动化浏览器方案。

* **技术路径**：
    * 通过 F12 开发者工具进行网络侦察，定位 XHR 请求。
    * 发现 4 个公开的 AJAX 接口（如 `ajax_findRecruitmentinfoLimitList`），无需鉴权即可获取结构化 JSON [cite: 4]。
* **核心优势**：直接请求 API 相比解析 DOM 树，速度提升了数倍，且数据准确度更高。这证明了在编写爬虫前，深入理解目标网站的通信协议比编写解析规则更重要。
